{
  "name": "auth-service-tests",
  "version": "1.0.0",
  "description": "Unit tests for auth service",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test dist/**/*.test.js",
    "test:verbose": "npm run build && node --test --test-reporter=verbose dist/**/*.test.js",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^20.19.9",
    "@types/jsonwebtoken": "^9.0.7",
    "@types/bcryptjs": "^2.4.6",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@prisma/client": "^6.13.0",
    "fastify": "^5.4.0",
    "jsonwebtoken": "^9.0.2",
    "bcryptjs": "^2.4.3"
  }
}
