{
  "name": "game-service-tests-extended",
  "version": "1.0.0",
  "description": "Extended unit tests for game service",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test dist/**/*.test.js",
    "test:verbose": "npm run build && node --test --test-reporter=verbose dist/**/*.test.js",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^20.19.9",
    "@types/ws": "^8.18.1",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@prisma/client": "^6.13.0",
    "fastify": "^5.4.0",
    "ws": "^8.18.0"
  }
}
